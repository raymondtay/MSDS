library(testthat)

# Problem 1: Let X be a random variable such that E(X) = 3, sd(x) = 4
# and let Y be a R.V. s.t. E(Y) = 1, sd(Y) = 2.
# X and Y are not linearly independent, and corr(X, Y) = 0.5
# (a) What is E(X + 3Y + 1)? and put into e1.1
e1.1 = NA
# your code here

# (b) What is sd(4X + 2Y + 2)? and put into sd1.2 .Round to two decimal places
sd1.2 = NA
# your code here


# what is E(3X -2Y - 10)? Put into e1.3
e1.3 = NA
# your code here

# What is sd(3X - 3Y - 5)? put into sd1.4, round to 2 decimal places
sd1.4 = NA
# your code here

# Problem 2
# TWo components neede in automotive manufacturing each can be represented in 1 of 3 grades.
# Let X be the cost of the first component (in hundreds of dollars) and let Y be the cost
# of the second component (also in hundreds of dollars). The following table represents the weekly
# fraction of cars that utilize each combination of these components.
#
# |  | y = 7 | y = 9 | y = 10 |
# |--| ---   |   --  |   --   |
# | x = 7 | 0.05 | 0.05 | 0.10 |
# | x = 9 | 0.05 | 0.10 | 0.35 |
# | x = 10 | 0.0 | 0.20 | 0.10 |
#
# (a) What is Cov(X, Y)? put into cov.xy 
cov.xy = NA
# your code here

# Support the RVs measure the cost of each part in dollars, rather than hundreds of 
# dollars. In this case, the RVs would be U = 100X, V = 100Y. What is cov(x,y) ? put into cov.uv
cov.uv = NA
# your code here


# What is the correlation coeffcient p(x, y)? Save into rho.xy
# what is the correlation coefficient p(u,v)? Save into rho.uv
rho.xy = NA
rho.uv = NA
